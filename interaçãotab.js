function tabuada() {
    let num = document.getElementById('txtn')               //variavel num ligada com o elemento do documento identificado por txtn
    let tab = documnet.getElementById('seltab')             //variavel tab ligada com o elemento do documento identificado por seltab
    if (num == 0) {                             //validacao: se o valor de num for 0 ou vazio escreva um alerta, se nao continue
        window.alert('Por favor, digite um número!')
    } else {
        let n = Number(num.value)                               //variavel n recebe o valor numerico de num
    }




}